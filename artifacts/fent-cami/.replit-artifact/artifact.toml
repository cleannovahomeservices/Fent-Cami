kind = "web"
previewPath = "/"
title = "Fent Camí"
version = "1.0.0"
id = "artifacts/fent-cami"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18184

[services.development]
run = "pnpm --filter @workspace/fent-cami run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fent-cami", "run", "build" ]
publicDir = "artifacts/fent-cami/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18184"
BASE_PATH = "/"
